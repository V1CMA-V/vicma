---
import HeroButton from '@/components/freelance/HeroButton.astro'
import { Image } from 'astro:assets'
---

<section id='hero' class='min-h-[75dvh] flex items-center justify-center px-4 md:px-8 animate-fade-in-up animate-duration-[1s] animate-delay-[0.5s]'>
  <article
    role='group'
    aria-label='Transformo ideas en soluciones digitales'
    class='max-w-6xl w-full m-auto py-6 px-6 md:px-12 bg-white/5 backdrop-blur-xl border border-white/10 rounded-3xl shadow-xl flex flex-col md:flex-row gap-6 overflow-hidden min-h-[60dvh]'
  >
    <!-- Texto -->
    <div class='flex-1 min-w-0 flex flex-col justify-center md:items-start space-y-4 text-text1'>
      <h1 class='text-3xl md:text-5xl font-bold leading-tight'>
        <span class='bling'>Transformo ideas en</span>
        <strong class='accent-bling'>soluciones digitales</strong>
      </h1>
      <p class='text-sm md:text-base text-text2 max-w-prose'>
        Soy un desarrollador web freelance especializado en construir experiencias modernas, rápidas
        y a medida para negocios como el tuyo.
      </p>
      <HeroButton>Contacto</HeroButton>
    </div>

    <!-- Imagen -->
    <div class='hidden md:flex flex-1 relative h-64 md:h-auto'>
      <Image
        src='/me.webp'
        alt='Foto de Víctor'
        width={600}
        height={700}
        loading='lazy'
        class='absolute inset-0 w-full h-full rounded-2xl object-cover'
      />
    </div>
  </article>
</section>

<!-- <style>
  @keyframes bye {
    to {
      translate: 400px 0;
    }
  }

  article {
    animation: bye linear both;
    animation-timeline: scroll();
  }
</style> -->
